# Changesets

Add a changeset with `npm run changeset` when a contribution changes a published package. The generated Markdown file records the affected packages, semantic version impact, and release note.

Documentation-only and internal maintenance changes normally do not need one. See [CONTRIBUTING.md](../CONTRIBUTING.md) for the complete policy.
