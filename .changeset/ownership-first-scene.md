---
"@alterno-dev/spatial-review-protocol": minor
"@alterno-dev/spatial-review": minor
"@alterno-dev/spatial-review-validator": minor
"@alterno-dev/spatial-review-cli": minor
---

Add the opt-in `scene-assemblies-v1` ownership contract, transform-only assembly
registration, parent-local actor poses, inherited visibility, and flattened
world-space fallback for consumers that do not negotiate hierarchy. Add ownership
validation, explicit assembly/placement feedback targets, migration guidance, and
fixtures. Existing world-space transform semantics and flat producers are retained.
Requires maintainer protocol acceptance before merge/release; this changeset does
not publish the implementation draft.
